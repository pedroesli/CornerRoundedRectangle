import XCTest
@testable import CornerRoundedRectangle

final class CornerRoundedRectangleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CornerRoundedRectangle().text, "Hello, World!")
    }
}
